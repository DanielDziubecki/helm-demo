apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "itdepends.fullname" . }}-test-connection"
  labels:
{{ include "itdepends.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "itdepends.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
