helm del $(helm ls --all --short) --purge


apiVersion: apps/v1
kind: Deployment
metadata:
  name: itdepends-deployment
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: itdepends-app
  template:
    metadata:
      labels:
        app: itdepends-app
    spec:
      containers:
      - name: {{ .Values.containerName }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env: 
        - name: mongo__ConnectionString
          value: {{ .Values.settings.mongo__ConnectionString | quote }}  
        - name: mongo__DatabaseName
          value: {{ .Values.settings.mongo__DatabaseName | quote }} 
        - name: ASPNETCORE_ENVIRONMENT
          value: {{ .Values.settings.ASPNETCORE_ENVIRONMENT | quote }} 
        ports:
        - name: {{ .Values.ports.name }}
          containerPort:  {{ .Values.ports.containerPort }}
       

        